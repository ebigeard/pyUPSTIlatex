# pyUPSTIlatex

Utilitaire pour lire, parser et éditer des fichiers UPSTI LaTeX.

Install:  
pip install -e .

CLI:  
pyUPSTIlatex inspect path/to/file.tex  
pyUPSTIlatex extract-zone path/to/file.tex zone_name
